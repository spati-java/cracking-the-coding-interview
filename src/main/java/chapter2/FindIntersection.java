package chapter2;


import model.ListNode;

/**
 *
 * Given Two Singly LinkedList determine if the two list intersect,
 * Return the intersecting node. Note that the intersection is defined based on the reference not value.
 * That is if the kth node of the first linkedlist is the exact same node (by reference) as jth node of the second linkedlist then the are intersecting;
 *
 * */
public class FindIntersection {

public ListNode findIntersection(ListNode l1 , ListNode l2){

        return null;

}

}
